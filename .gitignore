# Binarios compilados (se generan con go build; no deben versionarse)
/permcheck
/permcheck.exe
/scaletest
/scaletest.exe
/controller
/controller.exe

# Cualquier ejecutable de Windows
*.exe

# Archivos temporales de pruebas locales
*.txt.tmp

# Artefactos de cobertura de tests
*.out
coverage.*
